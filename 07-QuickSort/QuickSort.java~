import java.io.*;
import java.util.*;

public class QuickSort{
    Random r = new Random();
    public int[] quickSort(int[] a){
	if (a.length <= 1)
	    return a;
	else{
	    int p = a[r.nextInt(a.length)];
	    //insert partitioning here
	    int[] R1 = quickSort(R1);
	    int[] R2 = quickSort(R2);
	    //merge R1 + p + R2
	}
	//^clearly, I still don't understand...
    }
    public static void main(String[] args){
	QuickSort q = new QuickSort();
    }
}
